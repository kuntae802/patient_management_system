# Epic 8 회고 — 환자 포털 · 운영 대시보드 (★ 최종 에픽)

**일자:** 2026-06-26
**상태:** Epic 8 = **done (5/5 스토리)** · **로드맵 마지막 에픽 → 사실상 프로젝트 완주 회고**
**참여:** Player_kt (Project Lead) · Amelia (Developer) · Alice (Product Owner) · Charlie (Senior Dev) · Dana (QA)

---

## 1. Epic 8 요약 (지표)

| 항목 | 값 |
|---|---|
| 스토리 | **5/5 done** — 8.1 진료내역 · 8.2 처방·검사 결과 · 8.3 수납·영수증 · 8.4 APK 패키징 · 8.5 운영 대시보드 |
| 마이그레이션 | **2건**(0054 diagnoses.patient_friendly_note · 0055 examinations.patient_result_*) — 8.3·8.4·8.5는 **마이그 0** |
| 신규 권한 | **0** (전부 기존 권한 재사용) |
| 코드리뷰 | 5스토리 전부 통과 (8.1 patch1 · 8.4 patch2 · 8.5 patch3/defer2/dismiss6 적용 후 done) |
| 선행 블로커 | **0** (Epic 7 회고의 "Epic 8 착수 블로커 0" 예측 적중) |
| 회귀 | API 973 passed · web 541 passed (클린 DB reset 기준) |

핵심 성격: **"누적 데이터(Epic 4·5·7) 위 읽기 뷰 + APK + 대시보드"** — 5스토리 중 3개가 마이그 0. 상류 산출물을 탄탄히 깔아둔 배당금.

## 2. 잘된 점 (강점)

1. **환자 포털 self-read 패턴 확립(8.1) → 8.2/8.3 재사용.** `/patients/me/*` · `get_current_patient` · `auth_uid=sub` 경계 · service_role RLS 우회 경계선(소유검증 where절이 진짜 경계). 보안 임계 영역을 한 번 정립하고 일관 적용.
2. **재사용 극대화.** 8.3 마이그 0(7.5 `_assemble_receipt_payload` 추출해 직원/환자 공용) · 8.5 마이그 0·권한 0(집계 소스·`dashboard.read` 전부 기존). "신규 0"이 Epic 8 시그니처.
3. **의도된 이월의 계획적 청산.** 7.9 `refunded_amount_krw`가 8.5 순수납액 = Σ(paid−refunded)에서 첫 리포팅 소비(예고대로).
4. **미답 영역 정복(8.4).** 첫 Flutter 웹뷰 APK 릴리스 빌드 성공(43.6MB · 무-sudo Android SDK+Temurin JDK17 · PopScope 뒤로가기·로딩/오류 오버레이·`isInternalUrl` 내비 정책으로 셸 견고화).
5. **적대적 다층 리뷰의 가치 입증(8.5).** Blind/Edge/Auditor 3레이어가 스냅샷 시간축 불일치(`completed`=completed_at vs `visits`=registered_at → 자정 넘김 "완료 > 내원" 모순)를 출시 전 포착 → 코호트를 `registered_at`으로 통일해 **`내원 = 대기+진료중+완료` 불변식** 복원.

## 3. 개선점 (반복 마찰)

- **🔴 ruff E501 한국어 docstring — Epic 6→7→8 3연속 미해결.** 8.5에서도 백엔드·테스트·패치마다 반복 발생, 매번 수동 단축. CJK 너비로 100자 초과 잦음. 회고 액션(자동 래핑/공유 lint 헬퍼)이 세 에픽째 미이행 — 누적 비용.
- **🟡 dev DB 비결정성.** 8.2·8.5에서 `db reset` 후 auth/kong 502 + **누적 테스트 데이터 오염**. 8.5 리뷰 시 오늘자 registered 내원 457건 누적으로 billing/encounters 통합테스트 4건이 **가짜 실패** → 클린 reset 후 973 통과로 규명(코드 회귀 아님). 회귀 신호를 흐리는 위생 문제.
- **🟡 IDOR 소유검증 패턴 매-스토리 재구현(8.1/8.2/8.3).** self-scope는 정확했으나 공유 헬퍼화 미수행(반복 보일러플레이트).

## 4. 이전 회고(Epic 7) 후속 이행

| Epic 7 액션 | 상태 | 근거 |
|---|---|---|
| create-story 시 상태/게이트를 **실 소비경로 대조** | ✅ 이행 | 8.5 create-story가 `/admin/dashboard`(기존 nav)·`dashboard.read`(0002 시드)·집계 소스를 실제 코드와 대조 |
| 8.4 Flutter APK 스파이크 | ✅ 이행 | APK 빌드 성공 |
| 8.5 = refunded 첫 소비 | ✅ 이행 | 순수납액 = Σ(paid−refunded) |
| 횡단 정책 부채에 **소유 스토리** 라벨 | ⏳ 부분 | 8.5 deferred 2건 기록했으나 "해소 소유=Story X" 라벨 미적용(다만 둘 다 문서화 수용/Post-MVP라 종결성) |
| **ruff E501 (재시도)** | ❌ 미이행 | 8.5에서 또 비용 발생 |
| is_active soft-delete · 낙관적 동시성 TOCTOU | ⏳ 이월 지속 | DB 최종선 자세 유지(의도적) |

5개 능동 액션 중 3 이행·1 부분·1(E501) 미이행. 이행률 양호하나 E501 1건이 끈질김.

## 5. 다음 에픽 — 없음 (프로젝트 완주)

epics.md에 Epic 9 없음 → **Epic 8이 마지막**. 8개 에픽(플랫폼·마스터·환자·내원·오더·예약·수납·포털/대시보드) 전부 done = **PMS 스토리 레벨 완주**. 다음 에픽 준비 단계 해당 없음. 대신 **출시 전 실테스트(E2E)와 배포 반영**이 잔여 게이트.

## 6. 액션 아이템 (시간추정 없음)

**⭐ 최우선 (Project Lead 지정):**
- [ ] **실테스트 빡세게(E2E) — 새 세션 진행.** 6역할 직원 워크플로 + 환자 포털(웹/APK) + 운영 대시보드를 실제 흐름으로 골든패스 완주 검증. 스토리 레벨 done을 "실제처럼" 동작으로 최종 확정. _Player_kt 주도 · 별도 세션_

**프로세스/기술 부채:**
- [ ] (3번째 재시도) **ruff E501 한국어 docstring을 도구로 강제** — pre-commit 자동 래핑 또는 per-file 관용/line-length 조정 검토. 수동 단축 반복 종결. _Amelia_
- [ ] **dev DB 위생 관행** — 회귀 판정 전 `supabase db reset` + auth 폴링을 기본 절차화(누적 오염 가짜 실패 차단). 통합테스트 격리 일자(고정 과거/미래) 패턴 확산. _Dana/Amelia_
- [ ] **IDOR 소유검증 공유 헬퍼** — `where e.id=$2 and p.auth_uid=$1` self-scope 패턴을 db 헬퍼로 추출(8.1~8.3 보일러플레이트 통합). _Amelia_

**추적 유지(이월·종결성):**
- [ ] is_active soft-delete 일관 정책 (DB 최종선 결정상 계속 이월 가능).
- [ ] 임상/예약/오더 낙관적 동시성·TOCTOU (DB 최종선 유지).
- [ ] 8.5 대시보드 deferred 2건 — 과거일 waiting/in_progress 스냅샷 의미(문서화 수용) · 일자그룹 표현식 인덱스(성능 Post-MVP). `deferred-work.md` 등재.

## 7. 완료성(Readiness) 점검

| 영역 | 상태 |
|---|---|
| 테스트·품질 | ✅ 코드 그린 — API 973·web 541(클린 DB)·ruff/eslint/tsc/build 통과. ⚠️ **단, 통합 자동테스트 ≠ 실 E2E** — 사용자가 별도 세션에서 빡센 실테스트 예정 |
| 배포 | ⏳ 8.1~8.5 로컬 커밋(미푸시) — 클라우드(`kuntae802.mooo.com/patient_management_system`)에 8.4 APK·8.5 대시보드 반영은 푸시+compose build 필요(사용자 결정) |
| 산출물 위생 | ⚠️ `supabase/demo_seed.sql` git 미추적(8.5에서 커밋함) · 회고/스토리 산출물 커밋 완료 |
| 기술 건강 | ✅ DB-owns-invariants 견고 · 마이그 0001~0055 깨끗 · 이월 문서화 |
| 미해결 블로커 | **0** (스토리 레벨) — 잔여는 실 E2E 검증 + 배포 반영(사용자 게이트) |

## 8. 핵심 교훈 (Key Takeaways)

1. **상류 산출물을 탄탄히 깔면 하류 에픽은 "신규 0"이 된다** — Epic 8 5스토리 중 3개 마이그 0. 재사용 가능한 경계(self-read·영수증 조립)를 한 번 정립한 효과.
2. **적대적 다층 리뷰는 미묘한 정합 결함을 잡는다** — 8.5 자정 넘김 코호트 불일치는 자동테스트가 같은-날 데이터만 써서 못 잡았고 리뷰가 잡음.
3. **dev DB 비결정성은 회귀 신호를 흐린다** — 클린 reset이 진실. 누적 오염 가짜 실패에 속지 말 것.
4. **추적만으론 부족 — 반복 마찰엔 도구 강제가 답** — E501이 3연속 회고에 오른 건 "사람 주의" 액션의 한계. 자동화로 종결해야.
5. **"스토리 done ≠ 제품 done"** — 자동테스트 그린 위에 실 E2E·배포 반영이 마지막 1마일. 프로젝트 완주 회고에서 가장 정직해야 할 지점.

---

**Status:** Epic 8 회고 완료 · Epic 8 = done(5/5) · **8개 에픽 전부 done = PMS 스토리 레벨 완주.** 다음 = **실테스트 빡세게(E2E·새 세션)** → 배포 반영. 회고 액션 4건(⭐실테스트 · E501 도구화 · dev DB 위생 · IDOR 헬퍼).
